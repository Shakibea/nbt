import 'package:flutter/material.dart';

class MemberWrapper extends StatelessWidget {
  const MemberWrapper({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
